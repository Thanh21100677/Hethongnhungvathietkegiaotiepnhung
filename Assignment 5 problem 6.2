int main() {
    char *words[] = {"apple", "banana", "apple", "orange", "banana"};
    
    for (int i = 0; i < 5; i++) {
        node *n = lookup(words[i], 1);
        n->count++;
    }

    // in ra
    for (int i = 0; i < HASHSIZE; i++) {
        node *n = hashtab[i];
        while (n != NULL) {
            printf("%s: %d\n", n->word, n->count);
            n = n->next;
        }
    }

    cleartable();
    return 0;
}
