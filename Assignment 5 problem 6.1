int main() {
    person arr[] = {
        {"An", "Nguyen", 22},
        {"Binh", "Tran", 19},
        {"Cuong", "Le", 25}
    };

    int n = 3;

    // Sort theo firstname
    qsort(arr, n, sizeof(person), cmp_firstname);

    // Sort theo lastname
    qsort(arr, n, sizeof(person), cmp_lastname);

    // In người >20 tuổi
    apply(arr, n, isolder);

    return 0;
}
